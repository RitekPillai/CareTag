import 'package:flutter/material.dart';

class Watchregitrationpage extends StatelessWidget {
  const Watchregitrationpage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
